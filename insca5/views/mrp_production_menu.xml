<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Para desplegable Material -->
        <record id="action_material" model="ir.actions.act_window">
            <field name="name">Materiales</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">material</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Add Text Here
              </p><p>
                More details about what a user can do with this object will be OK
              </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="material" name="Materiales" parent="mrp.menu_mrp_configuration" action="action_material" sequence="101"/>

        <!-- Para desplegable Acabado -->
        <record id="action_acabado" model="ir.actions.act_window">
            <field name="name">Acabados</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">acabado</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Add Text Here
              </p><p>
                More details about what a user can do with this object will be OK
              </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="acabado" name="Acabados" parent="mrp.menu_mrp_configuration" action="action_acabado" sequence="102"/>

        <!-- Para desplegable Subconjunto -->
        <record id="action_subconjunto" model="ir.actions.act_window">
            <field name="name">Subconjuntos</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">subconjunto</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Add Text Here
              </p><p>
                More details about what a user can do with this object will be OK
              </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="subconjunto" name="Subconjuntos" parent="mrp.menu_mrp_configuration" action="action_subconjunto" sequence="103"/>

        <!-- Para desplegable Seccion -->
        <record id="action_seccion" model="ir.actions.act_window">
            <field name="name">Secciones</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">seccion</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Add Text Here
              </p><p>
                More details about what a user can do with this object will be OK
              </p>
            </field>
        </record>
        <!-- This Menu Item must have a parent and an action -->
        <menuitem id="seccion" name="Secciones" parent="mrp.menu_mrp_configuration" action="action_seccion" sequence="104"/>

    </data>
</odoo>